# Qwen3-VL + Regression Head for Robot Manipulation
model:
  name: "Qwen/Qwen3-VL-Embedding-8B"
  freeze_vision: true
  freeze_llm: false
  use_lora: true
  lora_r: 16
  lora_alpha: 32
  lora_dropout: 0.05
  lora_target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
  use_4bit: true
  bnb_4bit_compute_dtype: "bfloat16"
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_use_double_quant: true

data:
  dataset_name: "X-Humanoid/RoboMIND2.0-Franka"  # RoboMIND 2.0 Franka (ModelScope)
  data_dir: "./data"
  history_frames: 4
  action_dim: 7
  action_horizon: 1
  image_size: [224, 224]
  normalize_actions: true
  train_split: 0.9

training:
  output_dir: "./checkpoints"
  num_epochs: 10
  batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 1e-4
  weight_decay: 0.01
  lr_scheduler: "cosine"
  warmup_ratio: 0.1
  max_grad_norm: 1.0
  bf16: true
  gradient_checkpointing: true
  save_steps: 500
  eval_steps: 500
  logging_steps: 10
  dataloader_num_workers: 4
  seed: 42
